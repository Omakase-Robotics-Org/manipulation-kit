"""D1-branded end effectors (the stock hardware that ships on the D1).

"d1" plays the ``<maker>`` slot: the stock parallel gripper has no separate
maker branding, and registering it here keeps Shu's rule that the default
tool is one instance of the tool-config abstraction — data in
dx-manipulator, not a special case hardcoded in robot code.
"""
